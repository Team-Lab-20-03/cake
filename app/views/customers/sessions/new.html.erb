<div class="container">
  <div class="row">

    <h3 class="text-center"><strong>会員の方はこちらからログイン</strong></h3>
    <div class="col-xs-10 col-xs-offset-1 form-horizontal">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-group">
          <label class="col-sm-3 col-sm-offset-1">メールアドレス</label>
          <div class="col-sm-8">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %></div>
        </div>

        <div class="form-group">
          <label class="col-sm-3 col-sm-offset-1">パスワード</label>
          <div class="col-sm-8">
          <%= f.password_field :password, autocomplete: "current-password", class: 'form-control' %></div>
          <%= link_to "パスワードを忘れた方はこちら", new_password_path(resource_name), class: 'col-sm-8 col-sm-offset-4 text-right' %>
        </div>

        <div class="col-sm-12">
          <%= f.submit "ログイン", class: 'btn btn-primary col-sm-4 col-sm-offset-4' %>
        </div>
      <% end %>
      <div class="col-sm-11 col-sm-offset-1">
        <h4><strong>登録がお済みでない方</strong></h4>
        <%= link_to "こちら", new_customer_registration_path %>から新規登録を行ってください
      </div>
    </div>

  </div>
</div>